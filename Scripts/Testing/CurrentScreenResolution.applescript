tell application "Finder"
	set screen_resolution to bounds of window of desktop
	--position of first window of application process "MacPass"
	--size of the first window of application process "MacPass"
	--display alert screen_resolution using item 3 as text
end tell
